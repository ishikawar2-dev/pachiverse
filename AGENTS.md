# AGENTS.md — 全AI共通の作業ルール

このリポジトリは「リポジトリ自体をAIの外部長期記憶として育てる」思想を採用する。
Claude / Codex / Cursor / Copilot / Gemini その他、どのAIエージェントもこのファイルのルールに従うこと。

- `docs/` = プロジェクトの共通知識・長期記憶（全AI共有）
- `AGENTS.md` = 全AI共通の行動ルール（このファイル）
- AI固有設定（CLAUDE.md 等） = 共通知識への接続アダプター。プロジェクト知識を書かない
- Git = コードと知識を同期・永続化する基盤

---

## 1. 作業開始時のルール

1. この AGENTS.md を読む
2. `docs/00_OVERVIEW.md` でプロジェクト全体像を把握する
3. 今回の作業に関連する docs だけを読む（全部読む必要はない）
4. 関連コードを確認する
5. `docs/DECISIONS.md` に関連する過去の設計判断がないか確認する
6. `docs/KNOWN_ISSUES.md` に関連する既知問題がないか確認する

## 2. Source of Truth

- 現在実装の Source of Truth は「現在実際に動作しているコード」
- ただしビジネス要件・法的要件・セキュリティ要件・設計意図・変更禁止の制約は、コードで表現できないため docs を優先する
- コードと docs の矛盾を発見したら放置しない。コード・テスト・docs・DECISIONS を確認し、現在採用されている仕様に合わせて docs を修正する。判断不能なら推測で直さず「要確認」と明記する

## 3. 事実と推測の分離

docs には確認できた事実のみを書く。以下は禁止:

- ファイル名・関数名だけから仕様を推測して書く
- コードを確認せずに設計理由を創作する
- 過去の予定を現在仕様として書く
- 存在しないビジネスルールを補完する

判断できない場合は「未確認」「コードから判断不能」「要確認」と明記する。

## 4. コード変更前の確認

- 既存アーキテクチャと矛盾しないか
- 同じ機能・再利用可能な既存処理がすでにないか
- DB・認証認可・セキュリティ・他機能への影響
- 過去に却下された設計でないか（DECISIONS.md）
- KNOWN_ISSUES.md との関係

「より綺麗だから」という理由だけで既存設計を変更しない。ドキュメント整備タスクの最中に、依頼されていないコード変更を勝手に行わない。

## 5. 実装変更時のドキュメント更新

変更によって影響を受ける docs だけを更新する:

| 変更内容 | 更新先 |
|---|---|
| DB スキーマ | 02_DATABASE.md（存在すれば） |
| 認証・認可 | 03_AUTH.md（存在すれば） |
| API | 04_API.md（存在すれば） |
| システム構成 | 01_ARCHITECTURE.md |
| セキュリティ | 05_SECURITY.md（存在すれば） |
| 重要な設計判断 | DECISIONS.md |
| 既知問題の発生・解消 | KNOWN_ISSUES.md |

専門 docs（02_DATABASE.md 等）は必要になった時点で作成する。ファイル数を増やすこと自体を目的にしない。
軽微な文言修正や単純な UI 変更など、将来の AI に価値のない情報は記録しない。

## 6. 作業終了時の知識抽出

すべての作業の終了時に自問する:

> 今回新しく理解したことの中に、将来このプロジェクトを扱う AI が事前に知っていれば、調査・実装・デバッグ・設計判断を速く正確にできる情報はあるか？

ある場合のみ、適切な共通 docs へ反映する。

**保存する価値が高い知識**: コードだけでは理解しにくい重要仕様 / 非自明な依存関係（「AをいじるとBが壊れる」）/ 特殊なビジネスルール / 壊れやすい処理 / 認証・認可・セキュリティ上の制約 / 外部API固有の挙動 / タイムゾーン・冪等性・Retry・Race Condition / 一見不要だが削除してはいけない処理 / 採用しなかった設計と理由 / 本番環境特有の制約

**保存しない情報**: 日々の作業ログ / AIの思考過程 / 一時的な調査メモ / Gitで確認できる変更履歴 / 単純なコード説明 / 一時的なエラー / 細かなUI調整履歴

docs は作業日報ではない。追加前に確認する: 事実か（Accuracy）/ 将来役立つか（Relevance）/ すぐ無効にならないか（Durability）/ 適切なファイルか（Discoverability）/ 重複しないか（Non-duplication）。

## 7. DECISIONS.md の書式

```markdown
## Decision: タイトル（YYYY-MM-DD）
### Context
なぜ判断が必要だったか
### Decision
何を採用したか
### Reason
なぜ採用したか
### Alternatives
検討した代替案
### Consequences
メリット・デメリット・制約・将来への影響
### Status
Active / Superseded / Deprecated
```

コードだけでは「なぜこうしたか」が分からない判断を優先して記録する。設計変更後も過去判断を削除せず Superseded / Deprecated で状態管理する。

## 8. 現在仕様と履歴の分離

- 各専門 docs → 現在仕様のみ（古い仕様の履歴を追記し続けない）
- DECISIONS.md → 重要な設計判断と理由
- Git → 詳細な変更履歴

定期的に重複・古い情報・矛盾・冗長説明を整理する。ただし「最適化」を理由に、設計理由・セキュリティ制約・非自明な知識を勝手に削除しない。不明点を推測で補完しない。

## 9. セキュリティ

docs / Git に絶対に保存しない: パスワード / Secret / API Key / Access・Refresh Token / 秘密鍵 / Seed Phrase / 本番認証情報 / 個人情報 / 顧客機密情報。

環境変数は `DATABASE_URL`（接続先DB）のように**変数名と用途のみ**記載し、実値は書かない。

## 10. Git 運用

- AGENTS.md / docs/ / 共有可能な AI 設定は Git 管理対象（コードと同様のプロジェクト資産）
- コード変更でドキュメント更新が必要になったら、関連するコード・テスト・docs を可能な限り同一コミットへ含める
- AI は明示的な指示がない限り、勝手に commit / push しない。「commit 可能な整合状態」にするまでが AI の作業責務

## 11. Definition of Done

「コードが動いた」だけでは完了ではない。以下がそろって完了:

1. 実装 + テスト・検証 + 影響範囲確認
2. 影響を受ける docs の更新
3. 今回得た重要知識の抽出と docs への反映（該当があれば）
4. コード・テスト・docs が整合し、commit 可能な状態

## 12. AI 間の知識共有（Cross-AI Learning Loop）

重要知識を AI 固有ファイル・一時ファイル・AI 内部だけに残さない。

```
発見 → 共通 docs へ反映 → Git 管理 → 次の AI（どのツールでも）が利用 → さらに知識を追加
```

AI 固有ファイルを脳にしないこと。
